# RouletteSphereChaser v2 設計書 — 多塔パーク型ボールコースター（縦積み高密度版）

> 改訂3（2026-08-22）: User方針により塔をA〜Hの8塔に拡張。メダルゲームのJP抽選機・
> 初期パチンコ（「沼」式多段クルーン）・宝くじ抽選機（ガラポン）由来の皿・穴系機構を
> F/G/Hとして新設し、B/C/Eは演出安定のため搭載を軽量化。
> 改訂2（2026-08-22）: 全体高さ4倍（約14m級）・階層スタック構造。フェーズ1実装済み。

## 1. コンセプト

- 観賞用の循環型ボールパーク。**メインタワー1基＋サブタワー7基（計8塔）**、フットプリント**約21m×13m**（横3倍×奥行2倍拡張・2026-08-22）、**高さ約14m**。
- **造形方針: 抽選機・レール等の見た目はBlenderモデリングを主体**とする（演出のため。Unity側は物理コンポーネント・スコア・搬送を担当）。グレーボックスのプリミティブはプロトタイピング時のみ。
- ボールは径0.1のまま**36球**同時稼働（User指示 2026-08-23。フェーズ5までは24球運用）（番号0〜99アトラス・キャラテクスチャ差し替え・1巡スコア確定・カメラ追従対応）。
- 各塔は**3〜4ステージの縦積み**。ボールはリフトで頂部へ運ばれ、階層を順に降下しながら連続抽選を受ける。

## 2. 塔構成（8塔・抽選機構 約27基・縦積み 改訂3）

**設計方針**: F以降はD（クルーン塔）と同じ**皿・穴・ポケット系**の血筋で種類を増やす
（出典: F=メダルゲームのジャックポット抽選機、G=初期パチンコ「沼」式、H=宝くじ・福引きのガラポン）。
B/C/Eは演出の見栄え安定のため搭載を軽量化（E の回転ドラムはHへ移設・独立昇格）。

**レーン構造（2026-08-22確定・User案）**:

- **中央系（低配点・確実に次段へ）**: ミニルーレット内向き2口×4 → 落下環(r≈1.6) → 集約ファンネル → 大型ルーレット → 盆地。1周あたり 10/20 ＋ 10〜60 pt。
- **高レア系（高配点・横展開の起点）**: 外向き2口×4 → 金色HighLane 8本が四方へ（終端x/z≈±4.8で盆地落下）。1周あたり 80/120 pt。
  **将来の給球網**: +X 2本→C/E方面、−X 2本→B/D/H方面、+Z 2本→G、−Z 2本→F へ延長接続する（フェーズ4以降。延長時はレーン途中に受動分岐/シーソー等を挿入可）。

> **衛星4機の対称配置（User案 2026-08-22実装）**: JPスピナー=Z軸両端・沼=X軸両端で、高レアレーン8本すべてが機構に接続。
> B〜E/Hの給球は接続レール／第3リフト（3章）に戻す。建て込みは基準位置で行いグループごとY回転（`ParkBuilder`のyaw引数）。

| 塔                            | 位置(x,z)                                | 頂部高 | 階層（上→下）                                                                                                                                                                                                                                                                                                                                                                                                                              | 系統                                                                                                    |
| ----------------------------- | ---------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------- |
| **A: センタータワー**         | (0, 0)                                   | 13.5m  | ①分岐盤ディッシュ＋大スパイラル×4(13.5→9.6)✅ → ②③スパイラル毎チェーン×4: ミニクルーン(9.55→8.45)→ミニルーレット(8.45→7.2)✅ → ④**レーン分岐（User案）**: 内向き8口=低得点(10/20)→集約ファンネル(r2.05@6.15・撹拌付)→**大型ルーレット**(ボウルr1.7@5.0・スコア10-60)、外向き8口=高得点(80/120)→**高レア横ルート8本**(金色レーン・4°下り 7.1→6.95)✅ →回収フロア                                                                            | 看板・複合（抽選機11基）                                                                                |
| **B: パチンコ塔×2**           | (±6.6〜8.4, 0)                           | 4.3m   | Gハズレ合流トレイからの東行きトラフを受け→ピン盤→ステップチャッカー(4.3→2.2)→西行きトラフ→**D下段**へ                                                                                                                                                                                                                                                                                                                                      | 釘系（G系サルベージ・フェーズ6で移設）                                                                  |
| **C: ジグザグ塔×2**           | (x1.5〜6.5, ±6.05)                       | 4.96m  | Fハズレ受け→ジグザグ短縮版4段→シーソー(4.96→1.8)→**E下段（車輪ピックアップ）**へ                                                                                                                                                                                                                                                                                                                                                           | レーン系（F系サルベージ・フェーズ6で移設）                                                              |
| **D: クルーンボウル塔×2**     | (±5, 0) ＝**G塔の真下**                  | 3.09m  | **上段ボウル(2.65)=高得点チャレンジ**（G当選球のみ・JPレールで導かれる・5穴中1が当たり／他4穴は下段へ素通り） → **下段ボウル(1.60)=通常抽選**（B経由球＋上段のこぼれ・5穴中1が当たり）→回収フロア                                                                                                                                                                                                                                          | 皿・穴系（G系終端。主機構=クルーンボウル）                                                              |
| **E: 縦回転ポケット車輪塔×2** | (0, ±5) ＝**F塔の真下**                  | 4.11m  | **上段デッキ(3.94)=高得点チャレンジ**（F当選球・穴180／通過は車輪へ） → **縦回転車輪(中心2.31・φ1.76+カップ8)=投げ込み式抽選**（C排出球とも合流。カップ捕捉30%=WinTray55／弾かれ→**下段デッキ2枚直列(2.0/1.84)=通常抽選**(穴45/40・通過=回収フロア)）                                                                                                                                                                                      | 回転体系（F系終端。主機構=縦回転ポケット車輪。**リフト用途は廃止**=C排出3.1>デッキ2.0で不要と実測判明） |
| **F: JPスピナー塔×2**         | (0, ±5) ✅                               | 6.6m   | V字受けトレイ(6.25)→回転穴皿(5.55・中央JPカラー穴＋通過穴3=r0.32リング・15°/s)→セパレータ(4.65・中央チューブ=**JP150**／4°南北チルトのリング床→ギャップ→30)→回収フロア。**給球=HighLane±Z各2本**（レーン高6.95に合わせ中層化・実装済）                                                                                                                                                                                                     | 皿・穴系（メダルゲームJP式・D類似）                                                                     |
| **G: 沼塔×2**                 | (±5, 0) ✅                               | 5.6m   | **三連沼クルーン縦積みのみ**(5.10/4.30/3.55・**当たり=中央穴/ハズレ=各段の乖離シュート**・中心千鳥オフセットで同軸素通し防止・20/60/最終カップ110)→当選=D上段／ハズレ=B。**給球=HighLane±X各2本を最上段(L)へ直接投下**。**釘バラし盤は廃止**（フェーズ6・User指示 2026-08-23: 釘系はタワーBが担うので重複。ハズレルートの捌け口も簡潔になる。`TowerG_NumaBoard.fbx` は未使用残置）                                                         | 皿・穴系（初期パチンコ「沼」式・D類似）                                                                 |
| **H: ガラポン塔**             | (0, 0) ＝**中央・大型ルーレット直下** ✅ | 4.70m  | ①カラコロッタ皿(4.70・穴リング8＝3d/45°＋中央キャップ・当たり=東1穴**75**) → ②横軸ドラム(4.13・壁穴3=入口兼出口・25°/s) → ③2択スイング(3.30・東**100**)。**フェーズ6で回収を2ルートに分離（User案 2026-08-23）**: 〈赤〉**ハズレ回収皿**＝当たり穴以外の7穴の素通り球を全受け → 定方位へ排出／〈黄〉**塔本体の回収ルート**＝ドラム→スイングを抜けた球の受け樋 → 別方位へ排出。中央から球が降り注ぐ状態を無くし、B〜Eの設置スペースを空ける | 回転体系（宝くじ・福引き式。Eから移設し独立昇格）                                                       |

- **配置クリアランス（検算済み）**: A外周r≈3.25。F/G(0,±5)は塔半径≤1.3でAとの隙間0.45m=4.5d、
  H(-8.8,0)は塔半径≤0.9で西壁(x-10.05)と0.35m・B/D(x-7,∓4)と対角4.4m。いずれも並走壁基準1.5dを満たす。
- **塔間接続レール**: 上層帯（約10m）と中層帯（約6m）の2層に計8本。塔の途中階から隣の塔へ分岐する受動分岐（固定ペグ／Y字股。レーンを塞ぐ可動体は禁止）。
- 各階層の「こぼれ」はすべて盆地に落ちて循環継続（フェイルセーフ絶対則）。
- **新機構の物理注意**: 回転穴皿・回転ドラムはキネマティック回転体＝`ContinuousSpeculative` 必須（AGENTS罠14）。
  穴径はクルーン基準3.0d＋通過穴は2.2d以上。ドラム排出穴は内フィンで整流してから（アーチ防止）。

## 3. 回収・搬送系（フェーズ1で実装・検証済み）

- **矩形傾斜回収フロア（20.6×13m・3°・+X低）**＋周囲高壁（2.2m）＋**東側V字漏斗壁**で排水口(x≈10.5)へ集約 → 浅テーパー＋撹拌ローター → 2レーン分割 → **リフト2基（頂部14m・速度3.5）**。
- **広幅化（2026-08-22・罠12対応済み）**: 喉元を±0.40ポケット化＋撹拌ローター1.7倍(r≈0.43)・24°/s（先端は常に壁内=密閉掃引）。2レーン・リフトは無変更。12球でキュー5球→1〜2球。
- 注意: 傾斜フロアは必ずV字漏斗壁の合流点より東まで延長する（端で平地に落ちるとボールが停止する）。
- LiftN → タワーA頂部(0,13.5,0)、LiftS → タワーB頂部(-7,9.8,-4)へ投下。
- **給球計画（8塔版）**: C/E はAの中層帯レール（約10m/6m）から、F/G はAの①排出（9.6m帯）から分岐、
  D/H はB経由の西側レールから。西側（G/H/D）の球量が不足するようなら**第3リフト（西端 x≈-9.5）**を増設。
- 8球スモーク: 68周(3.4m版)、53周/130s(14m版)、50周/150s(横21m拡張版)。いずれも詰まり・場外ゼロ。

## 4. 安定稼働のクリアランス基準（d = ボール径 0.1）

| 部位                   | 最小値                                              |
| ---------------------- | --------------------------------------------------- |
| レーン内幅             | 1.6d                                                |
| 縦落下シャフト         | 2.0d                                                |
| すり鉢・ドレン開口     | 3.0d ＋ 撹拌ローター                                |
| 並走壁の隙間           | 1.5d（さもなくば0.5d未満に密閉）                    |
| 頭上クリアランス       | 1.2d                                                |
| 高所からの自由落下受け | 受け皿＋高壁（バウンド höhe = 落差×4%を封じ込める） |

AGENTS.md 3章の罠リスト全項目を `GreyboxKit` が強制（Cylinder凸メッシュ・トリガー・点数マーカー・撹拌ローター等）。

## 5. スコア設計（2026-08-22改定・確率反比例則）

- **配点則（User方針）**: 各トリガーの点数 = C ÷ P（P=1巡あたりの到達確率、C≈2.5）。「1巡あたりの獲得点数が、その点数まで獲得できる確率に反比例」する。各抽選機の当たりルートは**機内確率5〜15%**を目標に物理調整。
- **現行配点（2026-08-23・24球152周の実測Pで再計算。値は全トリガーで一意=ログ集計識別用）**:
  ミニルーレット全16口=10／大型ルーレット 通常10×3・当たり口(西)20（P12.5%実測）／
  F: スピナー通過15（P10.5%）・**JP150**（休止帯シムで難化）／G沼: k1=20・k2=60（P2.6%）・**最終カップ110**（P4.6%）／
  B: 西穴35（P7.2%）・中穴65・東穴90（各P1%未満）／C: シーソー通過25（P10.5%。実流は勢いで西へ抜けるため通過ゲート扱い）／
  H: カラコロッタ当たり穴75（P3.3%）・ガラポン→スイング東**100**（P2.0%）。実測平均 ≈30pt/巡。
- **沼クルーンv3「当たり穴＋ハズレ穴」方式**: 皿=リングトラフ＋中央クレーター縁（縁高=確率ノブ、現行0.035）。当たり=縁を越えて中央穴(3.0d)→採点、ハズレ=トラフ床穴×2(3.2d)から次皿へ素通り落下（採点なし・k3ハズレは盆地へ）。すり鉢単穴の「減速球は必ず中央=ほぼ必ず当たる」問題を構造で解消。穴・縁は3皿とも絶対寸法（L/M/S別メッシュ。一律スケールだとk3の穴が3d未満に潰れるため）。
- 塔B/C/E/H実装時も同じ反比例則で配点する。LapGateは排水路（両レーン共通）に設置済み。

## 6. 構築フェーズ

1. ~~**基盤**: 盆地・排水路・リフト2基（14m）~~ ✅ **完了**（commit `ea817db` ほか）
2. ~~**Tower A①**: 分岐盤＋大スパイラル×4~~ ✅ **完了**（commit `7792d34` ほか。全メッシュBlender製）
3. ~~**Tower A②③**: スパイラル毎のミニクルーン＋ミニルーレット×4チェーン~~ ✅ **完了**（8球全球スコア獲得・176秒20周・詰まり/場外ゼロ。初のスコア機構稼働）
4. ~~**Tower F / G**~~ ✅ **完了（F×2/G×2の対称4機構成）**、続けて✅**バランス調整**（5章の反比例配点則・沼クルーンv3・12球検証）と✅**排水路広幅化**（3章・16球運用へ引き上げ）まで完了（2026-08-22）
5. ~~**Tower B / C ＋ Tower H** → 24球検証~~ ✅ **完了（2026-08-23・User配置指示による設計変更あり）**:
   - **B/C はハズレルート接続型サルベージ抽選**として実装（塔独立ではなくF/Gの排出に接続）。
     B=パチンコ盤（G終端ハズレ受け・東西2基）、C=ジグザグ＋シーソー（Fリング床ギャップ排出受け・南北2基=Z鏡映配置）。
   - **H はタワーA中央チェーン3段目に挿入**: 大型ルーレット排出→カラコロッタ皿（穴リング8・当たり=東1穴）→
     ガラポンドラム（横軸・壁穴3=入口兼出口）→2択スイング→盆地。当初の単穴ファンネル案は多球アーチ＋煙突化で不通（AGENTS罠28）。
   - 併せて**スパイラル3巻き化＋下層一括+1.24詰め（LiftY定数）**・**LiftS両掛け化**（旧B予定地への空投下廃止）・
     **リフト半速化1.7m/s＋頂部搬送レール**・F皿の休止帯シム＋22°/s化。24球ソークで詰まりゼロ確認。
6. ~~**Tower D / E ＋ 接続レール残り**~~ ✅ **完了（2026-08-23）**。下記「フェーズ6 順路仕様」が正本。

   **フェーズ6 積み残し**
   - ✅ **`TowerG_MergeTray` 作り直し 完了（2026-08-24）** … 下記「フェーズ6.5」参照。
   - ✅ **F MissTray のスパウト脇 完了（2026-08-24）** … 下記「フェーズ6.5」参照。
   - **配点の再計算**（フェーズ7へ持ち越し）: 36球10分の実測Pで全トリガーを **点数=C/P** に揃える。
     計測は `ScoreZone.hits`（2026-08-24追加・NonSerialized）を `Unity_RunCommand` で読むだけでよい。
   - ✅ **縮退面／ゼロ法線の全FBXスイープ 完了（2026-08-24）** … 下記「フェーズ6.6」参照。使用中メッシュの縮退面・ゼロ法線は**すべて0**。
   - **`TowerG_NumaKuruun_L/M/S` の自己交差N-gon**（2026-08-24 実測・新規検出）: Unityインポータが
     **L=20枚 / M=22枚 / S=20枚のポリゴンを「self-intersecting」として破棄**している＝**コライダに穴が開いている**。
     3皿とも同型の欠陥（＝Sだけの問題ではない）。原因は非平面の大N-gon（23〜25角形）。
     Blender側で単純に三角化すると非平面ゆえに**面が張り替わって形が変わる**（実測 Δvol 3e-3・非多様体18→35）ので、
     **沼クルーンは作り直しが必要**（`TowerG_MergeTray` と同じ「閉じたソリッドをjoin」方式へ）。美化フェーズと同時にやるのが得。
   - **G/B/D系のさらなる嵩上げ**: `GLift` は0.30が上限。上げるには `TowerG_FeedCatch` の西壁を低くした非対称メッシュが要る。
   - **タワーHの2ルートの見た目**: 機能は成立。樋の造形（スパウト・受け口の化粧）は未着手。

### フェーズ6.5（2026-08-24）: 積み残し2件の解消 ＋ タワーE を「傾斜ポケット盤」へ全面差し替え

**① `TowerG_MergeTray` を作り直し（非多様体18本 → 0）**

- 作り方を変更: ブーリアン＋ベベルをやめ、**閉じたソリッドを個別に作って join** する（罠40）。
  床＝17×17のハイトフィールド格子＋底板（矩形ボア0.44×0.48を抜き、境界エッジを自動ブリッジ）／
  東西端壁・南北側壁・スパウト楣＝直方体／襟＝プロファイル回転体。**非多様体0・符号付き体積 +2.27**。
- **襟を円錐スカートに**（罠45の解決）: 内縁 r0.205（JPチューブ外径0.185 に対し隙間 **0.020 < 0.5d**）から
  r0.46 へ45°で下る。床から立ち上がる位置は実測 r0.37〜0.43＝矩形ボアの角(r0.325)を完全に覆う。
  楔ポケットが消えるだけでなく、45°面なので球が座れない。
- 床プロファイル: スパウト向き2.9°（k0.05）＋襟まわりは **迂回溝2本**（|y|0.62 に谷・中央は分水嶺）。
  スパウト端（x −0.95 以西）は単一V字に合流させる。**V字の横勾配は 1.9° → 5.5°（振幅0.030→0.085）**
  ―― 初版は浅すぎて西壁の隅に3球が座った（36球6分ソーク実測）。
- スパウト開口 3.4d → **5.0d（0.50）**、楣は床+0.30（3d）。

**② `TowerF_MissTray` のスパウト脇ポケットを埋める**

- フレア#1（`x+y=0.80` の稜）とリム（r0.755）の間に残る三日月ガターが袋小路だった。
  ここを **ショルダー（肩）ソリッド**で埋める: 方位20°〜87°、内縁=フレア稜(0.175)→外縁=リム天端(0.195)の
  緩い上り面。球が乗っても内側（フレア稜）へこぼれて皿床に落ちるだけで、留まる平場が無い。非多様体0を維持。

**③ タワーE を「45°傾斜ポケット盤」へ全面差し替え（User指示・参考: まちゃつ氏の自作ボール抽選機 YouTube `tVcMwzCRDcM`）**

旧「縦回転ポケット車輪＋デッキ3枚」は撤去（`BuildTowerE_Wheel` は参照用にコード残置）。

| 要素                   | 仕様                                                                                                                                                     |
| ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 盤 `TowerE_PocketDisc` | 外径1.00・板厚0.06・**リム壁0.55(5.5d)**・チャンネル r0.55〜0.88・ハブ円錐(基部0.55/頂0.34)                                                              |
| ポケット               | 8口・r0.68〜0.88 を貫通。**角幅で確率を作る**: 当たり9°／中14°×2／素通り22.5°×5                                                                          |
| ギア `TowerE_StarGear` | 11歯（ポケット8と非整数比＝毎回位相がずれる）・歯先0.72・谷0.60・内径0.20・厚0.10                                                                        |
| 回転                   | **皿＋穴＋ギア＋点数トリガーが一体で 18°/s**（`DiscPivot` にRotator。User訂正 2026-08-24）                                                               |
| 姿勢                   | 法線 (−sin45, cos45, 0)＝**低い側が −X（給球側）**。中心 world (1.45, 1.60, ±5)                                                                          |
| 給球 `TowerE_FeedPan`  | 固定。西高(−0.35, 1.95)→東低(1.02, 1.65) の12.4°シュート・壁0.45。JPチューブ(x0)の落下と C戻りトラフ西端排出をまとめて受け、盤の低い側へ0.5m落として投入 |
| JPチューブ             | E の受け面が下がったので **2段(1.6m)延長**（`tubeY` に 2.73 / 1.93 を追加）＝落差2.5m→1.3m                                                               |
| 排出                   | ポケットは盤を貫通し、球は裏面(−法線)へ抜けて盆地へ自由落下＝フェイルセーフ絶対則を維持                                                                  |
| 配点(暫定)             | 当たり **180**／中 **55**・**45**／素通り5口はトリガー無し                                                                                               |

**着工中に踏んだ罠（AGENTS 3章 46〜48 に追記）**

1. **機構FBXプレハブ直下に `Trigger()` をぶら下げると 100倍される**（プレハブ根 localScale=100）。
   20m角の巨大トリガーになり、6個が盤ごと球を飲み込んで **1000回超の多重加算**（実測 total 489,159 点）。
   → **スケール1の空ピボット `DiscPivot` を噛ませ**、回転体・メッシュ・トリガーをすべてその下に置く。
2. **ハブ円錐とギア内壁の環状すきま(r0.44〜0.54)が楔ポケット**。13球が r≈0.49 / c≈0.05 に座った。
   → ギア内径 0.54 → **0.20** にして円錐へ埋め、すきまそのものを消す。
3. **歯先とリムの隙間を球径未満(0.09)にすると、球が歯に噛んで皿と一緒に回り続ける**。
   回転体と球の相対運動が消えるので **ポケットに永久に落ちない**（10球が滞留・6分で入賞2回のみ）。
   → 歯先 0.79 → **0.72**（隙間 0.16＝1.6d・罠5の下限以上）。球は底に留まり、回ってきた穴に落ちる。

**36球ソーク実測（377秒・上記すべて反映後）**: 146巡・7,167点・**平均49.1/巡**・場外0・**恒常滞留0**
（低速判定に引っかかった5球はすべて排水路のキュー待ちで、機構内の滞留はゼロ）。
E のポケット入賞は6分で 45×5／55×2／180×3。給球パンの落とし口は盤の低い側 x0.60（r≈0.61 に着地）。

### フェーズ6.6（2026-08-24）: メッシュ健全性の総ざらい（別環境の `LiftGuide` 修正コミットの続き）

`Assets/Models/*.fbx` 全54ファイルを `check_mesh_health.py` と同じ判定式でスイープし、**使用中メッシュの縮退面・ゼロ法線を全て0**にした。
クリーンアップ手順は `LiftGuide` と同じ（`remove_doubles(1e-5)` → `dissolve_degenerate(1e-5)` → 残存ゼロ面積面を明示削除 → `recalc_face_normals`）。

| メッシュ | 修正前 | 修正後 | 形状不変の根拠 |
| --- | --- | --- | --- |
| `TowerA_Distributor` | 縮退2982/4599・ゼロ法線1178・非正規化12・非多様体52 | すべて0（1372面） | Δvol **4.2e-08**・バウンズ完全一致・**8方位×12点の床レイキャストが1点も動かず** |
| `TowerG_NumaKuruun_S` | ゼロ法線380・非多様体21 | ゼロ法線0（非多様体21は据置） | ジオメトリ無変更（シェーディングフラグのみ）・面数/バウンズ同一 |
| `TowerA_ScoreGate` | 縮退2 | 0（1046面） | Δvol 5.8e-11・バウンズ一致 |
| `ParkBase` | 縮退2 | 0（997面） | Δvol −2.8e-07・**15×15の床グリッド完全一致** |
| `DrainStation` | 縮退4 | 0（792面） | Δvol 1.9e-09・床グリッド完全一致 |
| `TowerE_TopSplit`（未使用） | 縮退4 | 0（144面） | バウンズ一致 |

**`TowerG_NumaKuruun_S` は縮退面0なのにゼロ法線380だった** ＝ 罠49とは別原因。
厚み **0.0046** のリム壁（r≈0.66・z0.18〜0.295 の384面）が**スムーズシェード**だったため、
内外の面法線が打ち消し合って頂点法線の長さが0になっていた。**当該バンドを `use_smooth = False` にするだけで解消**（頂点は1つも動かしていない）。

**新規検出（未修正・要作り直し）**: 沼クルーン **L/M/S の3皿とも**、Unityインポータが
「A polygon ... is self-intersecting and has been discarded」で **20枚前後のポリゴンを捨てている**＝**コライダに穴が開いている**。
原因は非平面の大N-gon（23〜25角形）。Blender側で三角化すると非平面ゆえに形が変わるので（実測 Δvol 3e-3・非多様体18→35）、
**`TowerG_MergeTray` と同じ「閉じたソリッドを個別に作って join」方式で作り直す**のが正解。美化フェーズとまとめてやる。

**36球ソーク（クリーンアップ後）**: 回帰なし（564秒・216巡・11,066点・平均51.2/巡・場外0）。分岐盤の見た目も継ぎ目の暗転が消えた。

**`LiftGuide` の残件（2026-08-24 調査結果）**: 縮退面0・ゼロ法線0・非多様体0・頂点法線の破綻0で、
メッシュとしては健全。ただし **Unityインポータが8枚のポリゴンを self-intersecting として捨てている**。
その8枚は **z=7.0 のチューブ端キャップ（8角形×8・32角形×4のうちの8枚）** で、
**円環状（穴あき）のN-gon**＝単純多角形ではないため三角化できない。ガイドチューブの内側にあるので見た目には出ない。
※三角化を試したら `ngon_method='BEAUTY'` が円環を潰して **Δvol −15%・非多様体0→82** になったので撤回済み。
直すならキャップを「内外2重リングの帯」として作り直す必要がある（沼クルーンの作り直しと同じ話）。
なお `weldVertices` の on/off では結果が変わらなかった（頂点数・縮退数とも同一）。

**併せて実施した調整（User指摘 2026-08-24）**

- **タワーE 給球パンの拡幅**: `TowerE_FeedPan` を 1.40×0.52 → **2.05×0.68**（壁0.45→0.50）にし、
  落とし口を x0.60 → **x0.95** へ。C戻りトラフの排出が遅いと着弾が東へ寄り、
  **旧パン東端(0.60)と盤の間の隙間からコースアウト**していた。いまは x−1.10〜0.95 が連続してパン、
  x1.00 から先は盤なので隙間ゼロ。排出初速 0.8／1.5／2.5 m/s の弾道すべてがパンに載ることを実測。
- **C戻りトラフの短縮**: `TowerC_CatchTurn` の長手だけ **0.8倍（5.77→4.61m）**。断面・高さはそのままなので
  勾配が 7.0°→8.7° になるだけで、**西端の排出点(x0.80・床3.03)は不変**。東端は 6.56 → **5.41**。
  設置xを 6.19 → **5.11** に寄せて西端を固定した。シーソー排出の弾道（初速1〜3m/s）は x4.51〜5.16 に着地し、
  バックストップ(5.0〜5.25)の内側に収まる。

### フェーズ8（予定・美化）: 配置管理のBlender移行「単一アセンブリFBX方式」（User合意 2026-08-24・次セッションの入口）

現状は ParkBuilder.cs が配置のSSOT（座標ハードコード＋パーツ別FBX）で、干渉の目視・配置調整・非破壊美化が難しい（User指摘）。
美化フェーズの最初に以下へ移行する:

1. **ブートストラップ（1回だけ）**: Unityの現行シーンから全機構のワールド変換を `Docs/park_layout.json` へダンプ →
   Blenderスクリプトが6つの原本.blendからオブジェクトをリンクして **`BlenderSources/ParkAssembly.blend`** を現行配置どおり自動組み立て。
   物理検証済みの座標を失わずに移行する。
2. **以後のSSOTは ParkAssembly.blend**: 配置調整・干渉検査（BVH重なり）・メッシュ美化をBlenderで行う。
3. **エクスポートは組み立て済み全体を1本のFBX**で出す。全オブジェクトが同一の軸変換を通るため、
   **罠19（X-mirror／bakeAxis 2系統）の per-part 座標規約が消える**＝Blenderの見た目がそのままUnityの配置になる。
4. **ParkBuilder は「解釈器」に縮退**（~200行）: アセンブリFBXをインスタンス化し、オブジェクト名／メタデータJSONから
   Rotator・MeshCollider・ScoreZone/MultMark・Stirrer・リフト等を付けるだけにする。
   - トリガーはBlender側の**Empty**で表現（FBX経由で空GameObjectになる）。回転盤の子トリガーは「皿の子のEmpty」。
   - 回転体には解釈器が**スケール1ピボットを自動で噛ませる**（罠46）。
   - スコア値・回転速度などの調整ノブはEmpty名（例 `T_score_180` / `ROT_18`）かメタデータJSONに持たせる。
5. 検証ループ（レイキャスト実測→36球ソーク）は従来どおりUnity側。移行直後に**旧シーンとの等価性確認**を必ず行う。

併せてやる美化系の懸案: 沼クルーンL/M/S・LiftGuideの自己交差N-gon作り直し（罠50）／タワーH樋の化粧／実機メダルゲーム風テクスチャ。

#### フェーズ8-1 完了（2026-08-24）: ブートストラップ＝`ParkAssembly.blend` の自動生成

上記手順1（ブートストラップ）まで実施。**配置SSOTはこの時点で `BlenderSources/ParkAssembly.blend` に移った**。

**生成物**

| ファイル | 中身 |
| --- | --- |
| `BlenderSources/ParkAssembly.blend` | メッシュ123＋機能マーカー123。メッシュは5つの原本.blendから**ライブラリリンク**（User選択・相対パス`//`）。Unityの階層をそのままコレクション階層に再現 |
| `Docs/park_layout.json` | Unity `ParkScene_v2` の全FBXインスタンス（パス／FBX／メッシュ名／localToWorldMatrix／ローカル・ワールドAABB／コライダ種別） |
| `Docs/park_markers.json` | ScoreZone(92)・Rotator(23)・Oscillator(5)・BallLift(2)・LapGate(1) の変換とパラメータ |
| `Docs/mesh_calib.json` | FBXごとの実測変換C（規約名・符号・スケール・両案の残差） |
| `Docs/mesh_stats_unity.json` / `mesh_stats_blender.json` | 校正の生データ |
| `Docs/build_park_assembly.py` | 上記から ParkAssembly.blend を組み立て直すスクリプト（冪等） |
| `Docs/verify_park_assembly.py` | 等価性＋AABB重なりの検証スクリプト |

**確定した座標規約（実測・38メッシュ全件で残差 ≤ 1e-6 m）**

- 「Blenderローカル頂点 → Unityメッシュローカル頂点」の変換 C は**2種類しかない**:
  - **base系** = `ParkBase.blend` の4オブジェクト（ParkBase / DrainStation / DrainStirrer / LiftGuide）: `C = s·diag( 1, 1, −1)`
  - **mech系** = **それ以外すべて**: `C = s·diag(−1, 1, 1)`（罠19の「X-mirror」の正体）
  - `s` はFBXインポータのスケール係数。旧世代FBXが `1.0`、新世代が `0.01`（プレハブ根 localScale=100 と対）
  - 2案が同値になるのは `TowerF_JPTube`（180°回転で不変＝無害）と `TowerH_Drum` のみ。
    ドラムは頂点集合の符号付きモーメント `Σz²x` をUnity/Blenderで突き合わせて **mech系と確定**（844頂点・完全一致）
- **アセンブリが採る唯一の全体規約 G**: `unity = (bx, bz, by)`（y と z の入れ替え・自己逆行列・det −1）。
  Blender標準の「+Z上／+Y前」→ Unityの「+Y上／+Z前」。以後 per-part の軸規約は不要。
- したがって各インスタンスの Blender 行列は **`T = G · M · C`**（M = Unityの `localToWorldMatrix`）。
  det(T) > 0 なので Blender 側に負スケールのオブジェクトは出ない。

**等価性検証の結果**

- 全123インスタンスのワールドAABB（中心・半径）が Unity と **最大 0.076 mm** で一致（＝float32ダンプの精度限界）。
- 全123マーカーの原点が **最大 3.8e-7 m** で一致。
- AABB重なりペア = **106**（同軸に重ねる設計＝ボウル＋ホイール等を含む。以後この数の増加を干渉の目安に使う）。
- 再読込テスト: リンク5本すべて相対パスで解決・非リンクメッシュ0。

#### フェーズ8-2 完了（2026-08-24）: 単一FBX化と `ParkBuilder` の解釈器化

**パイプライン（これが以後の標準手順）**

```
BlenderSources/*.blend（原本ジオメトリ）
        │ ライブラリリンク
        ▼
BlenderSources/ParkAssembly.blend  ← 配置・トリガー・回転体・得点表示のSSOT
        │ Docs/export_park_assembly.py
        ▼
Assets/Models/ParkAssembly.fbx           … メッシュ123＋マーカーEmpty
Assets/Models/ParkAssembly.params.json   … 名前 -> 付けるコンポーネントとパラメータ＋姿勢行列
        │ Tools > Build RouletteSphere Park (v2)（＝解釈器）
        ▼
Assets/Scenes/ParkScene_v2.unity
```

- `ParkBuilder.cs` は **1056行 → 約300行**。**座標を一切計算しない**。やることはメッシュへのマテリアル＋非凸MeshCollider付与、
  マーカーからのコンポーネント生成、`unity_path` からの階層復元、定点カメラ／スポナーの設置だけ。
- **規約Gの実現値**: FBX往復で180°ヨーが1回入る。`ParkBuilder.RootFix = Quaternion.Euler(0,180,0)` で戻すと
  `unity = (bx, bz, by)` が成立する（**90°刻み全24回転を総当たりして実測**・全123インスタンスで最大 **0.063 mm**）。
  ズレたときに直すのは**この1行だけ**。
- **罠51（新規）**: **UnityのFBXインポータは Empty(Null) ノードのスケールを「負値×100」で表現する**。
  取り込んだEmptyの `rotation` / `localScale` は信用できない（位置だけは正しい）。
  そのためマーカーの姿勢は `params.json` に Unity 座標の行列として持たせ、
  ParkBuilder は **JSONの位置とFBX Emptyの位置を突き合わせて 1mm 以上ズレたらエラーを出す**（規約ズレの自動検出）。
- 回転体はすべて **スケール1ピボット＋メッシュ子**に統一（罠46）。旧シーンでスケール100のまま回していた13基を是正。

**等価性検証（`Docs/diff_scene.py`・再実行可能）— 失敗0**

| 対象 | 結果 |
| --- | --- |
| メッシュ123 | ワールドAABBが旧シーンと最大 **0.06 mm** 一致。コライダ種別・マテリアル配分も一致 |
| 機能マーカー123 | 位置**完全一致**・回転誤差 4e-7・トリガー寸法一致・点数/倍率/回転速度/軸/揺動/リフト設定すべて一致 |
| 得点表示92 | 位置**完全一致**・回転誤差 3e-7・文字/サイズ/色/ビルボード一致 |
| 意図的な差 | 回転ピボット13基のスケールを1に統一（罠46） |

**36球ソーク（`Tools > Run Soak`／`Assets/Scripts/SoakRecorder.cs`）**

`SoakRecorder` は各トリガーの到達回数に加えて、**枠外へ出た球（コースアウト）・停止した球・
得点も周回もしないまま滞留する球**を記録する。結果は `Docs/soak_*.json`。

| run | シーン | hits | laps | score | **コースアウト** | 停止 | 迷子 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| before | 旧 | 311 | 121 | 9422 | **0** | 0 | 0 |
| before2 | 旧 | 280 | 118 | 7400 | **0** | 2 | 2 |
| after | 新 | 270 | 121 | 6408 | **0** | 2 | 1 |
| after2 | 新 | 288 | 119 | 7411 | **0** | 1 | 0 |
| after_10min | 新（10分） | 589 | 240 | 13727 | **0** | 1 | 1 |

- **コースアウトは4回とも0**。周回数も118〜121で揃い、循環は壊れていない。
- 停止・迷子は**旧シーンでも同頻度で出る**（旧2回目で停止2・迷子2）。しかも停止位置 (-0.93, 0.72, 0.26) は
  旧 `before2` と新 `after2` で同一＝**移行による回帰ではなく既存のアーチ詰まり**（罠4／罠23、
  タワーGクルーンの「2球同時到達でアーチ」懸念が実際に出ている）。フェーズ8の美化と同時に撹拌追加で潰す。
- トリガー到達数は塔単位で旧新一致（A:156/147 vs 154/152 など）。低トラフィックのタワーH・Gのみ小標本ゆえ揺れる。
  5分×2本では `TowerH/DropScore_100` が0だったが、**10分ソークでヒット1回を確認**＝機構は生きている（小標本のノイズだった）。
  10分ソーク（`Docs/soak_after_10min.json`）は92トリガー中61に到達。**フェーズ7の C/P 再配点はこのデータから始められる**。
- 構造検査: 回転ピボット28基すべて **コライダの `attachedRigidbody` 正常・スケール1・90°回転でコライダが追従**。

**以後の作業手順**

1. 形を変える → 原本 `.blend` を編集（`ParkAssembly.blend` はリンクなので自動反映）
2. 位置・トリガー・回転速度を変える → `ParkAssembly.blend` を編集
3. `Docs/verify_park_assembly.py` → `Docs/export_park_assembly.py` → Unityで `Tools > Build RouletteSphere Park (v2)`
4. `Tools > Run Soak` で回帰確認（**コースアウト0**を必須条件にする）

### フェーズ6.7（2026-08-24）: 高得点チャレンジの「倍率」化 ＋ D/E二段化の最終構造（User指示・タワー構造の最終調整）

**順路の最終形**: F/G当選 → **短縮したパイプ** → **高得点チャレンジ段** → （全穴が）→ **通常抽選段** → 盆地。
F/Gハズレ → C/B → **通常抽選段**（従来どおり）。チャレンジ段で外れても素通りにならず、必ず通常抽選へ合流する。

**得点処理の変更（User仕様）**

- 高得点チャレンジ段（D上段・Eチャレンジ盤）は**加点しない**。全穴が「**次の通常抽選で得る点数の倍率**」を与える。
  - 実装: `ScoreZone.grantMultiplier`（>0で倍率付与モード）＋ `LotteryBall.nextMultiplier`。
    通常段の加点時に `points × nextMultiplier` で消費（1回で1に戻る）。**1巡終了(CommitLap)でも失効**。
  - ビルダー側は `MultMark()`（水色の「xN」ラベル・Billboard付き）。HUDは保持中 `SCORE 10 x4` のように表示。
- 通常抽選段（D下段・E通常盤）は**全穴が加点**。チャレンジの倍率がここに掛かる。
- **D/E全8基の全穴に処理を付けた**（素通り穴の廃止。倍になった球の取りこぼしを無くす—User指示）。

| 機構 | 穴 | 割り当て（暫定・フェーズ7でC/P再計算） |
| --- | --- | --- |
| D上段（チャレンジ） | 5穴全部=倍率 | x6 / x5 / x4 / x3 / x2（入りにくい穴ほど高倍率・24球実測分布から） |
| D下段（通常） | 5穴全部=加点 | 52 / 48 / 44 / 32 / 28 |
| Eチャレンジ盤 | 8口全部=倍率 | 9°口=x6 / 14°口=x5, x4 / 22.5°口×5=x2 |
| E通常盤 | 8口全部=加点 | 9°口=180 / 14°口=55, 45 / 22.5°口×5=38, 36, 34, 33, 30 |

**構造の変更**

- **E**: 傾斜ポケット盤を**2枚**に。チャレンジ盤=中心(0.10, 3.85)・22°/s（`DiscPivotHi`）、通常盤=従来の(1.45, 1.60)・18°/s。
  F当選のJPチューブは**5段→3段に短縮**（下端4.37）。落下はチャレンジ盤のハブ斜面(4.11)に落差0.62で着地。
  チャレンジ盤の8口の排出はすべて下の**給球パン**に落ち（実測: z±0.3の帯で8方位ともパンにヒット）、
  通常盤へ流れ込む。パンは**西側半幅0.80→落とし口0.34のテーパー形**に作り直し（`TowerE_FeedPan` v3）。
- **D**: G当選のJPレール下段を**+0.40上げ**（下端3.26→3.66）、**上段ボウルも+0.40嵩上げ**（床2.70→3.10）。
  0.40が上限＝これ以上は下段への落差(0.88)が跳ね出し域に入る＆Gマージトレイ底(4.39)に迫る。
- 検証: テスト球を両当選ルートに注入し、**E: JP通過150→x2獲得→通常盤180が×2=360点**／
  **D: 上段x6→下段52が×6=312点** の全連鎖をログで確認。36球6分ソークで146巡・場外0・恒常滞留0。

- ✅ **タワーD 完了（2026-08-23）**: 上段ボウル床 world **2.40**（JPレール下端2.96の直下いっぱい。当たり穴 az54.5=**200**）／下段ボウル床 **1.13**・**盆地床と同じ3°チルト**（当たり穴 az162.5=**45**・yaw36で上段と半ピッチ千鳥）。各ボウルに**低速撹拌アーム**（DrainStirrer流用・35°/s）。32球シミュで全球回収・停留0。※フェーズ6.7で上段は倍率化＋0.40嵩上げ。
- ✅ 併せて修正: `TowerG_MergeTray` を−Z0.10してボアをJPチューブ軸に整列（落下球の停留解消）／`ParkBuilder` 末尾に `Physics.SyncTransforms()`／`TowerF_MissTray` に設置側4°チルト（実効≈7°）。

7. **負荷検証**: **36球**×10分連続、詰まりゼロ・スコア分布・エラー0（不足なら第3リフト追加）
   - 1回目（2026-08-23・608秒）: 203巡・4690点（平均23.1/巡）・場外0。**残課題3件**を検出 →
     ①D_W下段の下に球が潜り込む（→3°チルトで隙間0.132均一化・修正済） ②F MissTray床で停留（→4°チルト追加・修正済）
     ③**タワーG入口でHighLaneの高速球がFeedGuideを飛び越してコースアウト**（未修正。受け口を `TowerF_CatchTray`（F塔で実績のあるレーン受け）に置き換える案）
     ④**大型ルーレット当たり口(20)のScoreGate脇の隙間をすり抜けて素通りする球がある**（未修正。ゲート脇の隙間を塞ぐか、トリガーを飛翔弧全体を覆う大型箱にする＝罠31の適用）
   - **4回目＝フェーズ6の最終確認（700秒＝11.7分・36球）**: **261巡（球あたり2〜9巡）・8795点・平均33.7/巡・場外0**。
     恒常滞留は `TowerG_MergeTray` のボア襟の1件のみ（上記「積み残し」①②）。大型ルーレットのゲート脇すり抜けは、
     皿側の4スパウトの側壁を 0.055〜0.096 → **0.30（3d）** へ立ち上げて封じた（溝幅0.198＜ゲート開口0.29なので必ず枠を通る）。
     ※ゲート枠（`TowerA_ScoreGate`）を 0.39→0.68 に広げる案は、皿の縁との間にくぼみを作って
     **4門すべてで球を座らせた**（8球）ため撤回。「隙間は動く側でなく受け側（皿）で埋める」が教訓。
   - 3回目（494秒・下記の①〜⑤対応後）: **190巡・6150点（平均32.4/巡）・場外0・恒常滞留0**。
     対応内容（2026-08-23）:
     ① **F MissTray スパウト拡幅**（Blender）: 口(幅3d)の手前に**V字フレア2枚**を union（(0.66,±0.16)→(0.04,±0.78)）。
     側壁の外側に取り残されていた球が口へ導かれる。16球投下で 16/16 排出・停留0。
     ② **タワーG入口 受けパン新造** `TowerG_FeedCatch`: 飛翔方向の受け幅を 0.32 → **1.0** へ。V字床で
     中央(x5.72)へ集約し、6°チルトで皿の内側 r0.88 に落とす。**断面押し出し一発**で作り直して非多様体0。
     ③ **大型ルーレット20点ゲートのすり抜け**: ゲートのトリガーを 0.26角 → **0.40×0.42×0.40**（罠31）。
     ミニルーレット16門は内外0.32間隔のため既定サイズのまま（大型化すると二重加算）。
     ④ **タワーH 回収2ルート分離** `TowerH_MissPan` 新造: 実測の空き帯 y3.40〜3.60（上=ドラム底3.68 /
     下=スイング天面3.37）に外径2.6・リム0.14の受け皿を差し込み、+X側にドラム落下路のノッチ、
     **az180〜270 の90°開口**へ6°で流す。〈黄〉スイング通過球は `TowerB_CatchChute` 流用で東へ。
     ハズレ7穴の投下試験で 7/7 排出・停留0。
     ⑤ **タワーE 車輪の噛み込み**: 下段デッキAの西端が車輪カップの掃引円 r1.02 の内側(r0.86)にあり、
     カップと縁で球を挟んでいた。デッキA/Bを東へ+0.25（西端 r1.11）＋車輪の軸方向を1.45倍して
     ポケット隙間 1.5d → 約2.2d。
   - 2回目（537秒・上記①②修正後）: 197巡・4760点（平均24.2/巡）・場外0・**D下段の潜り込みは解消**。
     残る恒常滞留は **F MissTray のスパウト脇ポケット**のみ（3球）。実測プロファイル（F_N・z=4.69）:
     床は東へ 5.737→5.706 と下るが **x0.6〜0.7 で 5.787→5.835 の土手**（＝スパウトの側壁）に突き当たって止まる。
     スパウトの通り道は z=4.9〜5.1（床5.684）に限られ、**z方向の集約が2.9°と浅い**ため、
     z が0.2〜0.3ずれて降りた球が側壁の外側に取り残される。
     → 対策は**メッシュ側**（`TowerF_MissTray` のスパウト口をV字に広げる／側壁を漏斗状に寝かせる）が本命。
     設置側チルトでは x方向しか稼げず、この横ポケットは埋まらない。

### フェーズ6 順路仕様（2026-08-23 確定・着工の前提）

> v1の失敗をフェーズ6で再演した反省（AGENTS.md 1章）を受け、**順路を先に確定してから着工する**。
> 本節の表が「どの出口が次のどこへ入るか」の正本。座標は 2026-08-23 の実測（Unity側バウンズ／床レイキャスト）に基づく設計値で、
> 各機構の設置時に実測で微調整する。**フロア面 y ≈ 0.60 − 0.0524·x**（3°傾斜・+X低）。

**当落分岐の基本則（User確定）**: F/G で**当選した球は中間塔(C/B)を飛ばして終端塔(E/D)の高得点チャレンジ段へ直行**し、
**ハズレた球は中間塔(C/B)を経由して終端塔の通常抽選段へ**入る。終端塔 E/D は F系用・G系用にそれぞれ独立した塔として建てる（共用しない）。
**高得点チャレンジ段を外した球は下段の通常抽選段へ落ちて合流**する＝球ごとの経路状態を持たずに配点を分けられ、どこで外しても盆地へ落ちて循環が続く（フェイルセーフ絶対則）。

**F系（南北2基・F_S 基準／F_N は Z鏡映）**

| #   | 区間                                                                                                      | 出口→入口                                                                                                                                          | 位置(x,z)                        | y帯       |
| --- | --------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------- | --------- |
| F1  | F: 受けトレイ→回転穴皿(22°/s)                                                                             | HighLane∓Z×2 → 皿                                                                                                                                  | (0, −5)                          | 7.5→5.6   |
| F2  | **当選**: 中央JP穴→JPTube(貫通管)                                                                         | 皿中央穴 → E上段デッキ                                                                                                                             | (0, −5)                          | 5.6→3.9   |
| F3  | **ハズレ**: リング穴→MissTray（円錐勾配床＋分水嶺）→東スパウト                                            | スパウト(1.05, 床5.61) → Cジグザグ入口天面                                                                                                         | (0, −5)                          | 5.62→5.49 |
| F4  | C: ZigzagShort4段(x0.93〜3.47)→シーソー(3.65)→**戻りトラフ**(ジグザグ下をくぐる西行き5.77m・x−1.13〜4.63) | 西端排出(−1.15, 3.1) → E車輪西側ピックアップ                                                                                                       | z=−5（主面のまま・別レーン不要） | 5.49→3.1  |
| F5  | **E車輪=投げ込み式抽選**（φ1.76バンド+スコップカップ8・-20°/s西上昇。実装確定 2026-08-23）                | C排出球が東上面(≈(0.4,3.1))に着弾→**カップ捕捉(実測30%)=当たり**: 東下降で車輪際に低速リリース→WinTray(0.88, 2.30)=**55**／**弾かれ=下段デッキへ** | (0, −5)                          | 3.1→2.3   |
| F6  | **E上段デッキ（高得点チャレンジ・TopSplit両端開放版）**                                                   | JPチューブ直下(3.94)・東3°チルト・穴(0.32)=**180**／通過=東端→車輪東上面へ落下(F5と合流)                                                           | (0, −5)                          | 3.94      |
| F7  | **E下段デッキ2枚直列（通常抽選）**                                                                        | 弾かれ球の着地分布を受ける: A(0.85〜1.93, 穴=**45**)→B(2.35〜3.43, 穴=**40**)・通過=東端→回収フロア                                                | (0, −5)                          | 2.0/1.84  |

**G系（東西2基・G_E 基準／G_W は yaw180 ミラー）**

| #   | 区間                                                                                                                                                                                       | 出口→入口                                               | 位置(x,z)         | y帯                                             |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------- | ----------------- | ----------------------------------------------- |
| G1  | G: 沼クルーン3段のみ（**釘バラし盤廃止**）。給球=HighLane+X終端(4.82, 床8.20, z±1.5)の飛翔を**FeedGuide×2**(着弾窓5.30-5.62/6.95・6°中心チルト)で捕らえてL皿へ投下                         | レーン → FeedGuide → L皿                                | (5, 0)            | 8.2→6.6                                         |
| G2  | **ハズレ**: 乖離シュートは球を外へ放る（実測: 東6.3-7.5/西2.9-4.3の帯で降下）→ **高壁マージトレイ**(3.5×1.9・東西壁高2.3・User赤シールド案の発展形)が全周受け                              | シュート放出 → トレイ床（平面2.9°勾配+V絞り・襟迂回溝） | (5, 0.2)          | 6.3〜4.4→4.15                                   |
| G3  | **当選**: K3最終カップ(110) → **JPレール**(JPTube×2・トレイ中央ボア0.42角+襟を貫通)                                                                                                        | カップ → D上段ボウル直上(2.96)                          | (5, 0.2)          | 4.56→2.96                                       |
| G4  | マージトレイ東スパウト(開口3.4d・床4.15)                                                                                                                                                   | スパウト(6.78, 4.2) → B受けトレイ                       | x 6.68→6.78       | 4.15                                            |
| G5  | B: スパウト球を**ピン盤天面フレア**(盤 x6.27〜8.03・y2.36〜4.10)で直接受け（受けトレイ省略）→微振動±0.4°/1.3s→ステップチャッカー(1.81〜2.36・穴 西6.70=**35**/中7.18=**65**/東7.70=**90**) | 穴を抜けた球 → 直下のCatchChute                         | x 6.3〜8.0, z 0.2 | 4.15→1.81                                       |
| G6  | **B_CatchChute（新造）**: 全穴を覆う2.36m・側壁4.6d・東端バックストップ・西下り5°                                                                                                          | 西端排出(5.98, **1.65**) → D下段ボウル                  | x 6.00→8.36       | 1.78→1.60                                       |
| G7  | **D上段ボウル（高得点チャレンジ）**                                                                                                                                                        | 5穴中1=当たり／他4穴=下段ボウルへ素通り                 | (5, 0)            | 2.00（リム上端2.44）                            |
| G8  | **D下段ボウル（通常抽選）**                                                                                                                                                                | 5穴中1=当たり／他4穴=回収フロア                         | (5, 0)            | 1.11（リム上端1.55・B排出1.65がリム越えで投入） |

**G のハズレ受け構造（User案 2026-08-23・スケッチあり）**

- 3段の沼クルーンのハズレ穴を、**±X 方向の2枚のシールド受けで 3:3 に分担**して捕捉し、**当選穴のJPレールの真下で1本に合流**させる。
  ハズレルートが1本にまとまり、当選ルートとは交差しない（合流トレイは中央にボアを持ち、JPレールがそこを貫通して降りる）。
- **これは F の新構造と同じパターン**（周縁ハズレ＝中央ボア付きトレイ／当選＝中央貫通管）。前セッションでモデリング済みの
  `TowerF_MissTray`（2.0×1.6・中央ボアφ0.4）と `TowerF_JPTube`（φ0.37×0.8）を**Gの合流トレイ／JPレールに流用または派生**させる
  （新規モデリングは±Xのシールド2枚だけで済む見込み。L皿の外形は2.14なのでトレイ2.0はほぼ同径＝そのまま使える可能性が高い）。
- **要実測**: 現行 `TowerG_NumaKuruun_L` のレイキャスト計測では、各皿の貫通開口が**中央1＋外周4か所**（局所 x±0.15, z±0.378／r≈0.407）に見えている。
  Userの想定は各段2穴＝計6。**開口の数と実位置を細グリッドで確定してから**シールドの分割（3:3 か 6:6 か）を決める。
- B の高さ帯が G2 の合流点に従って下がる（旧 5.9→3.0 ⇒ **4.3→2.2**）。ピン盤＋チャッカーで約2.1m必要なので、受けトレイの落差を詰めて収める。
- D も連動して下がる（上段 3.4→**2.65**／下段 2.5→**1.60**）。D下段のリム上端2.04に対し B の連絡トラフ終端は2.15＝リム越えで投入する。
  上段ボウル底2.53 と 下段リム上端2.04 の隙間は0.49、上段の穴(r0.238)から落ちた球は下段ボウル(リムr0.86)の内側に収まる。

**H系（中央・フェーズ6で回収ルートを2分離。User案 2026-08-23）**

現状のHは、カラコロッタ皿の当たり穴以外（7穴）もスイングを抜けた球も、**中央から盆地へ降り注ぐ**。
これがB〜Eの設置スペースを塞ぐため、当選ルート（ドラム）から分離した回収構造を2つ足す。

| #   | 区間                                      | 出口→入口                                                   | 位置(x,z)                        | y帯       |
| --- | ----------------------------------------- | ----------------------------------------------------------- | -------------------------------- | --------- |
| H1  | カラコロッタ皿（穴リング8・中央キャップ） | 東1穴=当たり(75) → ドラムへ／他7穴 → **〈赤〉ハズレ回収皿** | (0, 0)                           | 4.70      |
| H2  | **〈赤〉ハズレ回収皿**                    | 7穴の素通り球を全受け → 定方位へ排出                        | (0, 0)・リムは皿の落下環を覆う径 | 4.3→3.6   |
| H3  | ドラム(壁穴3・25°/s)→2択スイング          | 東落ち=当たり(100)／西落ち                                  | (0.575, 0)                       | 4.13→3.30 |
| H4  | **〈黄〉塔本体の回収ルート**              | スイングを抜けた球の受け樋 → 〈赤〉とは別方位へ排出         | (0, 0)                           | 3.0→2.4   |

- **排出方位（第一候補）**: ±X は D（G塔の真下）、±Z は E（F塔の真下）が占めるので、**対角（az 45°／225°）へ流す**。
  〈赤〉と〈黄〉を別方位にして、樋どうしの交差を作らない。y2.4以下まで下ろせば、D下段ボウル底(2.38)の下をくぐって東へ抜ける経路も取れる。
- **フェイルセーフは維持**: 皿・樋の溢れは従来どおり盆地へ直落ち。回収構造は「降り注ぎを一方向へまとめる」だけで、球を閉じ込める形にしない。
- **新規Blenderメッシュが必要**（受け皿＋2本の樋）。既存の `TowerH_CatchFunnel.fbx`（フェーズ5で未使用残置）を下敷きにできるか先に確認する。

**配置の根拠（実測 2026-08-23）**

- **D/E は給球元(G/F)の真下の空きスペースに建てる**。G_E のバウンズ下端は y=4.5、F_S は y=5.61 で、いずれも直下から床まで 4m 級が空いている。
  当選球は真下へ落ちるだけで高得点チャレンジ段に入るので、**長距離レールを引かずに済む**（反省②の「長距離レールの絡まり」対策）。
- 東側フロア（x 6.5〜10・|z|≤3）は障害物なし・床 y 0.10〜0.26。西側（x −10〜−6.5）は床 y 0.94〜1.13。南北の縁ストリップ（|z| 5.8〜6.4・x 2〜8）も空き。B/C はこの帯に収まる。
- 中間塔の高さ帯は終端塔の入口高さから逆算する。**C: 終端 2.66 ⇒ 1.8**（E車輪ピックアップへ）。**B: 4.1→1.7 ⇒ 4.3→2.2**（Gハズレ合流点4.2から受け、D下段リム2.04を越えて投入）。
- **G釘バラし盤の廃止に伴う注意**: 盤が担っていた「投下位置の拡散」が無くなり、HighLane終端(x/z≈±4.8・y6.95)からの**固定方位投下**になる。
  AGENTS罠18（投下点の固定オフセットは分配方位バイアスになる）に該当しうるので、L皿への投下は**中心寄り＋ジッタ**にするか、
  レーン終端に短い受け樋を1枚挟んで方位を散らす。盤の撤去で L皿リム(≈6.84)とレーン床(6.95)のクリアランスが0.11しかないため、
  レーン終端の位置とリムの干渉も設置時に実測すること。

**使用メッシュの割り当て（フェーズ6コミット `5a75fc2` で資産化済み）**

| 塔  | 主機構（固有・流用禁止）                                                                                                                                                       | 搬送/汎用部品（流用可）                          |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------ |
| D   | `TowerD_Kuruun`（φ1.72・5穴リング d≈0.18@r0.238・中央ドーム）を**2段**                                                                                                         | `TowerDE_DiscBase` / `PickupTray` / `FeedTrough` |
| E   | ~~`TowerE_TopSplit`×2 ＋ `TowerDE_BigWheel`~~ → **2026-08-24差し替え**: `TowerE_PocketDisc`（φ2.0・45°傾斜・ポケット8）＋ `TowerE_StarGear`（11歯・同回転）。旧2点は未使用残置 | `TowerE_FeedPan`（固定給球パン・E専用）          |
| F   | `TowerF_MissTray`（中央ボア） ＋ `TowerF_JPTube`（貫通管・φ0.37×0.8）                                                                                                          | ― （旧 `TowerF_Separator` は廃止）               |
| C   | `TowerC_ZigzagShort`（2.55×0.56×1.54）                                                                                                                                         | `TowerC_CatchTurn` / `TowerC_Seesaw`             |

**配点（暫定・36球ソークの実測Pで再計算する）**: 高得点チャレンジ段は通常段より到達確率Pが小さいぶん高配点になる。
暫定値 D上段=200／D下段=45／E上段=180／E下段=55（全トリガー一意値・ログ集計の識別子を兼ねる）。

**着工順（1系統ずつ通す。反省②）**: F系 F→C→E を通し検証 → G系 G→B→D を通し検証 → 接続レール残り → 36球ソーク。
機構を1基置くごとに バウンズ監査＋頂点間実距離＋レイキャストで接続確認し、全系統を並べてから直そうとしない。

## 7. ファイル構成

- `Assets/Editor/ParkBuilder.cs` … v2ビルダー（`Tools > Build RouletteSphere Park (v2)`、冪等）
- `Assets/Editor/GreyboxKit.cs` … 共通ヘルパ（罠対策標準装備）
- `Assets/Scenes/ParkScene_v2.unity` … v2シーン（v1は`ParkScene_v1.unity`として凍結保存。2026-08-22 User改名: SampleScene→ParkScene_v1, ParkScene→ParkScene_v2）
- `Assets/Editor/GitTools.cs` … `Tools > Git Commit All`（定期コミット用）
- `BlenderSources/TowerA.blend` … タワーA機構（SpiralA/DistributorA/AgitatorA）✅
- `BlenderSources/ParkBase.blend` … 土台一式（ParkBase/DrainStation/LiftGuide）✅
- Blender追加予定: 大クルーン／ルーレット盤（A②③）、回転穴皿＋受けクルーン（F）、釘バラし盤＋三連クルーン（G）、回転ドラム＋スイング受け（H）、ウィッシングウェル（D）ほか（`BlenderSources/`。Z-up規約・Union＋ベベル仕上げ）

> **造形ポリシー（2026-08-22確定）**: シーン配置メッシュは全てBlender製（不可視トリガーのみUnityプリミティブ）。
> タワーA①実装詳細: 分岐盤=静止ディッシュ（リム床12.85・ノッチ4・radialスナウト緩下り）＋回転撹拌腕（4枚羽根＋円錐キャップ18°/s・Speculative CCD）。スパイラル4台は中心距離2.15m・yaw=30°-方位角。ボール分配はLiftN中心投下＋`releaseJitter=0.6`で一様化（実測14:15:8:11）。
