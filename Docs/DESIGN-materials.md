# DESIGN-materials.md — 外装（マテリアル／配色）設計書

> フェーズ9「美化」の配色・質感の正本。造形（メッシュ）の正本は `DESIGN-v2.md`、運用ルールは `AGENTS.md`。
> **決定日 2026-08-24（User合意）**: 色の割り当て軸＝**役割ごとに統一**／
> 第1段は**単色＋PBR数値のみ**（テクスチャ・装飾は本書で設計してから着手）。
> **2026-08-24 改訂（User指示）**: プロダクトのコンセプトを明文化し、**中性色（白・銀）基調へ後退**。
> 初版の「煌びやかなメダルゲーム筐体」は**アクセントが強すぎた**——理由は下の0章。

---

## 0. プロダクトのコンセプト（配色判断の上位ルール）

**想定するユーザーは2種類。どちらも「店には行かない人」である。**

1. **騒がしいゲームセンターやパチンコ店には入りにくいけれど、ボール抽選機は眺めていたい人**
2. **ギャンブルやアミューズメントを、軽く疑似体験したい人**

つまりこのアプリは**実機の代わりではなく、実機に行かずに済むための場所**。
ここから外装の判断基準が全部決まる。

| 実機（ホール）の言語 | 本アプリが採る言語 | 理由 |
|---|---|---|
| 射幸心を煽る原色・点滅・強い発光 | **中性色（白・銀・ガンメタ）と、静かな1色のアクセント** | 「入りにくい」理由そのものを持ち込まない |
| 当たりを最大音量で祝う | **点が入ったことが"読める"だけで十分** | 疑似体験は"軽く"がちょうどいい |
| 台の主張で客を引く | **球（キャラクター）が主役、機構は舞台装置** | 眺めるための画は静かなほど長く見ていられる |
| 中が見えない筐体（ガラス越し・裏は死角） | **機構の中身が見えることを最優先**（透過シェル・窓抜き） | 「眺めていたい」が第一の動機だから |

**この表と矛盾する外装案は、見栄えが良くても採らない。**
迷ったときは「静かな部屋でずっと流しておけるか」で判断する。

---

## 1. 世界観と狙い

薄暗い部屋に置かれた、静かなボール抽選機。**筐体はガンメタで沈み**、搬送路は白、台は銀、
抽選盤だけが深い紺——**色味を持つのは抽選盤ひとつ**で、あとは明度と質感の差だけで役割を語る。
光るのは「点が入る場所」だけ、それもやわらかい白。

設計上の狙いは4つ。

1. **25基あっても迷子にならない**。色は塔ではなく**役割**で決める。
   彩度を捨てたぶん、**明度3段（白レーン／銀の台／ガンメタ搬送）＋唯一の色味（紺の抽選盤）**で読ませる。
2. **静かであること**（0章）。強い原色・強い発光・点滅は使わない。アクセントは得点部の弱い白光だけ。
3. **中が見える**こと。球を囲っている造形は透過シェルか窓抜きにする（罠52の死角対策と同じ話）。
4. **ボール（キャラクター球）が主役として立つ**。背景側の彩度を抑え、球のテクスチャが最も明るく見えるようにする。

---

## 2. 役割の分類と塗り分け（第1段＝実装済みの値）

URP/Lit の `_BaseColor` / `_Metallic` / `_Smoothness` / `_EmissionColor` のみで作る。テクスチャは貼らない。

| # | 役割 | 意味 | BaseColor | Metallic | Smooth | Emission |
|---|------|------|-----------|----------|--------|----------|
| 1 | **CABINET**（筐体・構造） | 床・排水部・背板。世界を沈めるガンメタ | `#272C36` | 0.25 | 0.35 | — |
| 2 | **CHROME**（汎用搬送） | トラフ・チュート・ガイド・受け皿。明度3段の一番暗い段 | `#5E6773` | 0.90 | 0.55 | — |
| 3 | **LANE**（高レア搬送・旧GOLD） | HighLane 8本。**白**。金メッキは主張が強すぎたので廃止 | `#F2F4F7` | 0.35 | 0.72 | — |
| 4 | **JACKPOT**（JP経路） | JPレール／JPチューブ。**白**のまま、暖色を極僅かに含ませて他レーンと区別 | `#FBFCFE` | 0.35 | 0.78 | `#FFF2E0` ×0.20 |
| 5 | **DECK**（抽選盤・皿） | クルーン／ボウル／分配盤／沼皿。**唯一の色味**＝落ち着いた紺 | `#33407F` | 0.12 | 0.58 | — |
| 6 | **ROTOR**（回転体・台） | ホイール・ギア・ドラム・撹拌腕・シーソー。**銀**。赤は廃止 | `#C6CBD2` | 0.95 | 0.75 | — |
| 7 | **ROTOR_HI**（高得点回転体） | Eチャレンジ盤など「倍率が付く盤」。ROTORより明るい銀 | `#DCE2E9` | 0.95 | 0.82 | — |
| 8 | **SCORE**（得点ゲート・当たり口） | 点が入る場所。**唯一光る**が、やわらかい白に後退 | `#E8F1FA` | 0.20 | 0.85 | `#DDF2FF` ×0.70 |
| 9 | **SHOWPIECE**（大スパイラル） | 見せ場。乳白アクリルの内照。水色は廃止 | `#EDF1F5` | 0.05 | 0.90 | `#9FB0C0` ×0.20 |

> **廃止した色とその理由（2026-08-24・User指摘「アクセントが激しすぎる」）**:
> 金 `#FFC53C`（HighLane）と水色 `#9EE8FF`（大スパイラル）→ **白**へ。
> 赤 `#C8262C`（回転体）と橙 `#FF7A2A`（JP・Eチャレンジ盤）→ **銀／白**へ。
> 得点ゲートのシアン発光 ×1.6 → **やわらかい白 ×0.70**。
> 彩度で役割を分けるのをやめ、**明度と金属感で分ける**方針に切り替えた（0章の表に従う）。
> 色味を持つのは DECK（紺）だけ。ここを増やしたくなったら、まず0章を読み直すこと。

### マテリアル → 役割の割り当て

| 役割 | マテリアル |
|------|-----------|
| CABINET | `ParkBase` / `DrainStation` / `TowerB_PachiBoard` |
| CHROME | `LiftGuide` / `TowerF_CatchTray` / `TowerF_MissTray` / `TowerB_CatchTray` / `TowerC_CatchTurn` / `TowerC_Zigzag` / `TowerG_MergeTray` / `TowerH_CatchFunnel` / `TowerE_Pickup` |
| LANE | `TowerA_HighLane` |
| JACKPOT | `TowerG_JPRail` / `TowerF_JPTube` |
| DECK | `TowerA_Distributor` / `TowerA_MiniKuruun` / `TowerA_MiniRouletteBowl` / `TowerA_GrandRouletteBowl` / `TowerA_CollectorFunnel` / `TowerA_RouletteBowl` / `TowerA_GrandKuruun` / `TowerD_Kuruun` / `TowerG_NumaKuruun` / `TowerG_NumaBoard` / `TowerH_KarakoDish` / `TowerF_SpinnerDish` / `TowerF_Separator` / `TowerE_Deck` |
| ROTOR | `TowerA_MiniRouletteWheel` / `TowerA_GrandRouletteWheel` / `TowerA_RouletteWheel` / `TowerE_Gear` / `TowerE_Disc` / `TowerE_Wheel` / `TowerH_Drum` / `TowerH_Swing` / `TowerC_Seesaw` / `Greybox_Accent`（撹拌腕） |
| ROTOR_HI | `TowerE_DiscHi` |
| SCORE | `TowerA_ScoreGate` / `TowerB_StepChucker` |
| SHOWPIECE | `TowerA_Spiral` |

> **例外を作らない**こと。「この塔だけ色を変えたい」は塔ごと配色への逆戻りで、25基では必ず読めなくなる。
> どうしても差を付けたいときは**役割を1つ増やす**（ROTOR_HI がその例）。

---

## 2.5 透過アクリルのシェル（＝罠52「死角70%超」への回答）

コンセプト0章の「**中が見えることを機構の見栄えより優先する**」の実装。
球を囲っている外皮を**透過アクリル**にして、造形を削らずに中を見せる。

### なぜ窓抜き（造形を削る）ではなくこれなのか

- **どこを抜けばいいのかを実測したら、抜けない場所ばかりだった**。
  `CameraCoverage` に「何に遮蔽されたか」の集計を足して測った結果が下表。
  上位は**ファンネルの内壁・皿の外周壁・パチンコ盤の板**——**どれも球の通り道そのもの**で、
  穴を開ければ球が出る。縁を下げれば罠23（縁高＝確率ノブ）・罠44（脇抜け）に直撃する。
- 透過なら**コライダは一切変えない**ので、確率にも搬送にも影響しない。実測: 36球ソークで回帰なし。
- 実機のパチンコ盤・抽選機も前面はアクリル。**造形としても嘘がない**。

### 遮蔽の実測（`Docs/camera_coverage.json` の `blockers`・150秒36球）

| カメラ | 死角率 | 最大の遮蔽者（回数） |
|---|---|---|
| `Cam_A_GrandRoulette` | 0.844 | **CollectorFunnel 770** / GrandRouletteBowl 216 / KarakoDish 91 |
| `Cam_A_Overview` | 0.766 | **MiniKuruun 2328** / SpiralA_225 881 / MiniRouletteBowl 632 |
| `Cam_B_Pachinko_E` | 0.879 | **PachiBoard 49** / CatchChute 43 / StepChucker 16 |
| `Cam_C_Zigzag_N` | 0.776 | **Zigzag 46** / ReturnTrough 46 / Seesaw 29 |
| `Cam_H_Garapon` | 0.708 | **KarakoDish 88** / MergeTray 29 / MissPan 20 |

> **教訓**: 死角対策は「見た目で怪しい壁を削る」ではなく、**遮蔽者を数えてから決める**。
> `Cam_A_GrandRoulette` の犯人は大ルーレット自身ではなく**上に載っている集約ファンネル**だった。
> 盤の縁を下げていたら、確率を壊したうえに死角も直らなかった。

### 透過にしたマテリアル（正本は `ParkBuilder.SeeThroughMats`）

`TowerA_CollectorFunnel` / `TowerA_MiniKuruun` / `TowerA_MiniRouletteBowl` / `TowerA_GrandRouletteBowl` /
`TowerB_PachiBoard` / `TowerG_NumaKuruun` / `TowerH_KarakoDish`（皿・盤 = alpha 0.22）、
`TowerC_Zigzag` / `TowerC_CatchTurn` / `TowerG_MergeTray` / `TowerB_CatchTray`（搬送トラフ = alpha 0.28）。

- URP/Lit の Surface Type = Transparent / Blend = Alpha / ZWrite off / renderQueue = 3000。
- **盤面に印字するラベル（`GateLabel`）は盤の色で決まる**。得点ゲートを白にしたので、文字は抽選盤と同じ藍
  `#33407F`（`ParkBuilder.GateLabelColor`）。宙に浮く `DropLabel` は暗い背景の上なので `park_labels.json` の色のまま。
  **盤の色を変えたらラベル色も見直すこと**（白地に白＝読めない、を一度やった）。
- **平らな盤面の機構は正面からしか中が見えない**。定点カメラを360°周回させると一周のほとんどが裏＝死角になるので、
  `OrbitCamera.azimuthAmplitude`（配置方位を中心に往復する振り子）を使う。パチンコ盤は ±38°。
- **`SeeThrough` レイヤ**（index 8）を付ける。`CameraCoverage` のレイキャストはこのレイヤを除外するので、
  死角の実測値が「人間に見えるか」と一致する。**物理は素通しにしない**（レイヤは描画と計測のためだけ）。
- レイヤ付けは `ParkBuilder` が `SeeThroughMats` を見て自動で行う。増やすときはその集合に足すだけ。

### やらなかったこと

- **縁の低背化**は見送り。罠23（縁高0.055/0.035が当選確率のノブ）と罠44（低い側壁は跳び越えられる）に直撃する。
  透過で死角が解消するなら、確率に触る改造をする理由がない。
- **窓抜き**も見送り。上表のとおり遮蔽者が球の通り道と一致していて、安全に抜ける壁が無かった。
  透過後も残る遮蔽（塔の外枠・デッキ板など球の通り道でないもの）が出てきたら、そこは窓抜きの候補になる。

---

## 3. シーン側の設定（第1段＝実装済み）

| 項目 | 値 | 理由 |
|------|----|------|
| Ambient | Flat `#141821` / intensity 1.0 | ゲーセンの薄暗さ。Skybox環境光だと全体が青白く浮く |
| Skybox | 暗いProceduralスカイ（Exposure 0.15） | 背景を黒に近付けつつ、金属の反射先を完全な黒にしない |
| Directional Light | intensity 0.55 / 色 `#C8D8FF`（寒色） | メインは弱く。輪郭は金属反射に任せる |
| ArcadeSpot N / S | Spot 2灯・上方から俯瞰・暖色 `#FFD8A0` | 天井スポット。構造の可読性を担保する |
| Global Volume | Bloom（threshold 1.0 / intensity 0.9） | 発光している「点が入る場所」だけを滲ませる |

**注意**: これらは `Park` ルートの外に置く（`ParkBuilder` の再ビルドで消えない）。
ライトを `Park` の子にすると再ビルドのたびに消える。

---

## 4. 第2段の設計（テクスチャ・装飾）— **未着手・着工前にUser確認**

第1段の単色で配色バランスを確定させてから着手する。順番を守ること（テクスチャを先に貼ると、
色が悪いのかテクスチャが悪いのか切り分けられなくなる）。

### 4.1 前提（守る制約）

- 各メッシュは**単一マテリアル＋UV展開済み**（`AGENTS.md` 造形ポリシー）。スピンメッシュは **u=周方向 / v=断面** で展開してある。
  → **周方向に繰り返す帯パターン**（ストライプ・番号帯・ローレット）はUVと相性が良い。写真的なテクスチャは向かない。
- テクスチャは `Assets/Textures/` に置き、**PIL による手続き生成**（`Docs/gen_*.py`）で作る。
  再現性が要るので手描きPNGを直接置かない（ボールスキンだけは例外＝User手描き・git管轄外）。
- **1回に1機構ずつ**貼って見た目を確認する（`AGENTS.md` 反省2「まとめて作ってから直そうとしない」）。

### 4.2 貼るもの（優先順）

| 優先 | 対象 | 内容 | 生成方法 |
|------|------|------|----------|
| 1 | SCORE ゲート盤面 | 点数の数字を焼き込んだ発光パネル（現行はTMPラベルを別途重ねている） | `gen_score_panel.py`（Penchant書体・点数別に10種） |
| 2 | DECK 抽選盤 | 穴の周りに**当たり/ハズレの色環**と点数目盛り。ルーレット盤のフレット割り | `gen_deck_dial.py`（u=方位に等分割の扇形） |
| 3 | CHROME 搬送 | ヘアライン金属（uに沿った微細な条線）＋端部の縞（安全帯） | `gen_metal_hairline.py`（1px条線ノイズ＋タイル） |
| 4 | GOLD/JACKPOT | ローレット（滑り止め）と光沢のむら | 同上のパラメータ違い |
| 5 | CABINET | パンチングメタル（丸穴の等間隔）＋角の擦れ | `gen_punch_panel.py` |
| 6 | ROTOR | 回転方向を示す**矢羽根**と、掴み位置のゴム帯 | `gen_rotor_stripe.py` |

### 4.3 装飾（メッシュ追加を伴うので DESIGN-v2 側の管轄）

以下は**マテリアルではなく造形**の話なので、着工は `ParkAssembly.blend` への追加として扱う。

- 筐体外周のフレーム／幕板（いまパークは"浮いている"ので、床と壁で囲うと筐体らしくなる）
- 得点表示まわりのベゼル（TMPラベルが板から浮いて見えるのを抑える）
- タワーH樋の化粧（`AGENTS.md` フェーズ6積み残し）
- **罠52で「造形が球を囲っていて死角70%超」と判定された機構**（パチンコ盤・沼クルーン・大ルーレット）の
  **窓抜き・縁の低背化**。美化と可視性を同時に解決するので、装飾より先にやる価値がある。

---

## 5. 実装メモ

- マテリアルは `Assets/Materials/*.mat` の実体を書き換える。`ParkBuilder.Mat()` は
  **既存の .mat があればそれを返す**（無いときだけ生成する）ので、ビルダー再実行で色が戻ることはない。
- `Docs/park_materials.json` の `rgb` は **Blender側のビューポート色**であって、Unityの見た目には使われない
  （`ParkBuilder` は `.mat` を優先する）。Blenderでの作業性のために後追いで揃えてよいが、正本は本書。
- Emission を使うマテリアルは `EnableKeyword("_EMISSION")` と
  `globalIlluminationFlags = MaterialGlobalIlluminationFlags.RealtimeEmissive` を忘れない（付け忘れると光らない）。
